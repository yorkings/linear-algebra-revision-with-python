### linear algebra with python

---
 **The Building Blocks — Vectors & Matrices**

- A scalar → just a single number (like 5, -3.2, etc.)

- A vector → a list of numbers that gives direction + magnitude (like an arrow)

- A matrix → a grid (2D array) that can transform vectors (e.g. rotate, stretch, project)